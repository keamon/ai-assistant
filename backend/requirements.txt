# Pip-installable mirror of pyproject.toml, for tooling that expects requirements.txt
# instead of uv. The source of truth is pyproject.toml — regenerate this file if that
# changes: `uv export --no-hashes --no-dev -o requirements.txt` (from backend/).
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
google-adk>=1.15.0,<2.0.0
python-dotenv>=1.0.0
anthropic>=0.78,<1.0
litellm>=1.83.7,<=1.83.14
